{
  "name": "@galvanized-pukeko/demo-mcp",
  "version": "0.0.1",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "dev": "npm run build && node dist/mcp-http.js",
    "test": "vitest run",
    "lint:oxlint": "oxlint  --fix -D correctness --ignore-path .gitignore",
    "lint:eslint": "eslint  --fix",
    "lint": "oxlint  --fix -D correctness --ignore-path .gitignore && eslint . --fix"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "gaunt-sloth-assistant": "^1.0.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.5"
  }
}
